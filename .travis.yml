language:
  - swift
osx_image: xcode9
script:
  - xcodebuild -scheme SwiftExample -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone X,OS=13.5' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
